const state = () => ({

})

const getters = {
  getter(state) {

  },
}

const mutations = {
  commit(state) {

  },
}

const actions = {
  async dispatch({ state, getters, commit, dispatch }) {

  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
