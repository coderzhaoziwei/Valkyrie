const state = () => ({

})

const getters = {
  getter(state) {

  },
}

const mutations = {
  commit(state) {

  },
}

const actions = {
  init({ state, getters, commit, dispatch, rootState }) {
    dispatch("on", {
      type: "",
      handler(data) {

      },
    })

  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
